# EOL jig agent dependencies (Python 3.11+)
pylink-square>=1.3      # J-Link SWD/RTT (flash check, eoltest, prov wipe)
python-can>=4.3         # PCAN CAN stimulus (can_stim.py)
websockets>=12.0        # ws://localhost:9151 bridge to the /eol page
bleak>=0.22             # Phase-2 provisioning over the PC's BLE radio
